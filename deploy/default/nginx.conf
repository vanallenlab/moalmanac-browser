server {
    listen 80;
    server_name dev.moalmanac.org www.dev.moalmanac.org;

    location / {
        include proxy_params;
        proxy_pass http://unix:/home/breardon/moalmanac-browser/moalmanac-dev.sock;
    }
}